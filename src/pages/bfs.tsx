import { Flex } from '@chakra-ui/core'
import { BFS } from '../components/Bfs'

const bfs = () => {
    return (
        <Flex direction="column" my={20}>
            <BFS/>
        </Flex>
    )
}

export default bfs

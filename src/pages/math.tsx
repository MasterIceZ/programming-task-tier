import { Flex } from '@chakra-ui/core'
import { Math } from '../components/Math'

const math = () => {
    return (
        <Flex direction="column" my={20}>
            <Math/>
        </Flex>
    )
}

export default math
